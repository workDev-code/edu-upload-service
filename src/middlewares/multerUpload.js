import multer from "multer";
import { mimeTypesFilter } from "./mimeTypesFilter.js";

/**
 * Factory tạo instance multer cho từng loại file
 * @param {string} type - image, video, audio, document
 */

const multerUploader = (fileCategory) => {
    
};
